services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: always

# Note: You can also add a postgres service here if you follow 
# the instructions in backend/POSTGRES_GUIDE.md
