services:
  - type: web
    name: jobfit-backend
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: LLM_NAME
        value: openai:gpt-4o
      - key: OPENAI_API_KEY
        sync: false # Set this in Render dashboard as a secret
    healthCheckPath: /
